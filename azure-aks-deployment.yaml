apiVersion: v1
kind: Namespace
metadata:
  name: aks-dev-ns
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-api-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-api
  template:
    metadata:
      labels:
        app: todo-api
    spec:
      containers:
      - name: todo-api
        image: myAksRepo.azurecr.io/todo-api
        ports:
        - containerPort: 5209
        env:
        - name: CorsAllowedHosts
          value: "*"
---
apiVersion: v1
kind: Service
metadata:
  name: todo-api-backend
spec:
  type: NodePort
  ports:
  - port: 5209
    targetPort: 5209
    nodePort: 30001
  selector:
    app: todo-api